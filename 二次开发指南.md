# Flux 二次开发指南

> **项目定位**:面向 V2Board 机场站长的"白标客户端"。Flutter 3.x 全平台壳 + Xray / sing-box 双内核,支持 Android / iOS / Windows / macOS / Linux,带商业化(订阅、订单、邀请、客服)闭环。
>
> **快速心智模型**:
> - **UI 层**(纯 Dart):`lib/`
> - **代理协议引擎**:Android 用 `libxray.so` + `libhev-socks5-tunnel.so`(VpnService);桌面端用外部进程 `xray` / `sing-box`;iOS 走 `NetworkExtension` 模板。
> - **业务后端**:V2Board REST API(`lib/services/v2board_api.dart`)。
> - **可远程下发的运行时配置**:OSS 上的加密 `release_config.json`(`lib/services/remote_config_service.dart`)。

---

## 一、技术栈与依赖速览

来自 `pubspec.yaml`:

| 层 | 关键依赖 | 用途 |
|---|---|---|
| 框架 | Flutter SDK `>=3.5.0 <4.0.0`,Material 3 | UI |
| 网络 | `http: ^1.2.0` | 调用 V2Board API、下载订阅与远程配置 |
| 持久化 | `shared_preferences`、`path_provider` | Token、域名、TUN 状态 |
| 桌面 | `window_manager`、`tray_manager`、`process_run` | 窗口/托盘/启动 sing-box·xray 子进程 |
| 加密 | `encrypt: ^5.0.3` | V2Board 邮箱验证 AES-128-ECB |
| 客服 | `crisp_chat: ^2.4.3` | 仅 Android/iOS 内置 Crisp |
| 国际化 | `flutter_localizations`、`intl` | 中英两套 ARB |
| 解析 | `yaml: ^3.1.2` | 解析 Clash YAML 订阅 |
| 打包 | `msix`(Win)、`flutter_launcher_icons`(图标) | dev 工具 |

> ⚠️ 项目里把 `process_run` 用于桌面端拉起 `xray`/`sing-box.exe`。这是理解代理执行链的关键。

---

## 二、目录结构总览

```
flux/
├── lib/                              # Dart 业务全部在此
│   ├── main.dart                     # 入口,初始化窗口/托盘/退出清理/AuthGate
│   ├── l10n/                         # 国际化 ARB + generated
│   ├── models/                       # 8 个数据模型(plan/server_node/user_info...)
│   ├── screens/                      # 10 个页面
│   ├── services/                     # 14 个服务,核心逻辑层
│   ├── theme/                        # 颜色/主题
│   ├── utils/                        # 工具类(加密、UA、节点工具、资源拷贝)
│   └── widgets/                      # 22 个自定义控件
├── android/                          # VpnService、Xray、TUN(hev-socks5-tunnel)
├── ios/                              # NetworkExtension 模板 + VPNManager
├── macos/ linux/ windows/            # 桌面端 runner + 打包脚本
├── assets/
│   ├── bin/{linux,macos,windows}/    # 内置 sing-box / xray / wintun.dll
│   ├── bin/geoip.dat,geosite.dat     # 路由库
│   ├── icons/, images/               # 图标、截图、捐赠二维码
├── encrypt_config.py                 # XOR+Base64 加密脚本
├── release_config_plaintext.json     # 远程配置明文模板
├── routing_rules.json                # 分流规则模板
├── CONFIG_README.md                  # 简版配置说明
├── analysis_options.yaml, l10n.yaml, pubspec.yaml
└── test/widget_test.dart             # 仅一个占位测试
```

---

## 三、开发环境搭建

### 1. 工具链
- **Flutter** 3.x(稳定版,Dart `>=3.5`)。
- **Android**:Android Studio + JDK 11(`build.gradle.kts` 强制 `VERSION_11`)、`compileSdk=36`、`ndkVersion=27.0.12077973`。
- **iOS / macOS**:Xcode 15+、CocoaPods。
- **Windows**:Visual Studio 2022 (含"使用 C++ 的桌面开发")、Inno Setup(打包安装包,使用 `windows/flux_setup.iss`)。
- **Linux**:`clang`、`cmake`、`ninja-build`、`libgtk-3-dev`。
- **Python 3** (跑 `encrypt_config.py`)。

### 2. 第一次拉起来
```bash
git clone <你的 fork>
cd flux
flutter pub get
flutter gen-l10n            # 生成 lib/l10n/generated/*.dart
flutter run                 # 默认 debug,跑当前平台
```

### 3. 国际化生成
`l10n.yaml` 已配置:
```yaml
arb-dir: lib/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
output-dir: lib/l10n/generated
```
新加文案:在 `lib/l10n/app_en.arb` 和 `lib/l10n/app_zh.arb` 同步加 key,运行 `flutter gen-l10n`。

---

## 四、核心模块讲解

### 4.1 入口与启动流程

`lib/main.dart` 第 23-97 行:
1. `WidgetsFlutterBinding.ensureInitialized()`。
2. `_registerExitCleanup()` 注册:
   - 桌面端通过 `windowManager.addListener(_AppCloseListener)`,**关闭窗口前先 `vpn.disconnect()`**(否则 TUN 网卡留垃圾)。
   - Unix 端监听 `SIGINT`/`SIGTERM`,同步断 VPN 后 `exit(0)`。
3. 桌面端额外:`V2rayService().loadTunState()` 读取上次 TUN 开关、`windowManager.ensureInitialized()`、`TrayService.init()`、设置窗口图标(优先 `data/flutter_assets/`,fallback 开发路径)。
4. Android 端调用 `AssetUtils.copyAssets()`,把 `geoip.dat / geosite.dat` 拷到 `filesDir`,供 native xray 使用。
5. `runApp(FluxApp)` → `AuthGate`。

`AuthGate` 看到 token + authData → 调 `getUserInfo()` 验证 → 进 `RootShell`,否则进 `AuthScreen`。**预加载**关键数据(远程配置 + 公告)以让 splash 动画期间数据就绪(`main.dart` 第 202-218 行)。最小开屏时间 2500 ms。

### 4.2 三层服务架构

```
┌──────────────────────────── UI (screens/widgets) ──────────────────────────┐
│                                                                            │
│  HomeDashboard / PlansScreen / OrdersScreen / AccountScreen / InviteScreen │
│                                                                            │
└────────────────────────────────────┬───────────────────────────────────────┘
                                     │
┌────────────────────────────────────▼───────────────────────────────────────┐
│                              Service Layer                                 │
│                                                                            │
│  ApiConfig ──► RemoteConfigService ──► OSS 加密配置                        │
│       │                                                                    │
│       ▼                                                                    │
│  V2BoardApi (HTTP)        UnifiedVpnService                                │
│       │                       │                                            │
│       │            ┌──────────┴──────────┐                                 │
│       │            ▼                     ▼                                 │
│       │       V2rayService          SingboxService                         │
│       │       (Xray + 平台)         (Tun 模式专用)                          │
│       │                                                                    │
│       ▼                                                                    │
│  SubscriptionService / UserDataService / LatencyTestService / CrispService │
└────────────────────────────────────┬───────────────────────────────────────┘
                                     │
┌────────────────────────────────────▼───────────────────────────────────────┐
│                         Platform Channel / Native                          │
│  Android: MainActivity + FluxVpnService + libxray.so / libhev-socks5-...   │
│  iOS:     VPNManager + PacketTunnelProvider                                │
│  Desktop: Process(xray / sing-box.exe) + system proxy + tray               │
└────────────────────────────────────────────────────────────────────────────┘
```

### 4.3 远程配置 `lib/services/remote_config_service.dart`

- 单例;关键常量在文件顶部(第 20-26 行):
  - `_ossUrls` —— 多 OSS 优先级备份。
  - `_defaultDomain` —— 当远程全失败时的兜底域名。
- 核心方法 `getActiveDomain()` (第 50-84 行):
  1. 内存缓存 → 2. SharedPreferences 缓存(`remote_config_active_domain_v2`)→ 3. 拉远程配置遍历 `domains` 探测可用 → 4. 兜底 `_defaultDomain`。
- 缓存 6 小时(`_cacheValidHours`);`refreshActiveDomain()` 强刷。
- 远程内容统一经 `ConfigEncryption.smartDecrypt` 解密(自动识别明文/密文)。
- 还提供 `fetchRoutingRules()` 拉取 `routing_rules.json`,被 V2rayService / SingboxService 注入到 outbounds。

### 4.4 V2Board API 层 `lib/services/v2board_api.dart`

- 所有 HTTP 调用统一通过 `_get / _post`,会自动:
  - 拼 `getBaseUrl()`(取自 ApiConfig → RemoteConfig)+ `/api/v1` 前缀;
  - 注入 `authorization`、`User-Agent` (动态由 `lib/utils/user_agent_utils.dart` 生成);
  - 失败抛 `V2BoardApiException`(拿 statusCode 做 fallback,如 `getPlans` 在 404 时退到 `/guest/plan/fetch`)。
- **AES-128-ECB 加密邮箱验证**:`sendEmailVerify`(第 97-130 行):
  - Key 在 `_emailVerifyKey`(必须 16 字节)。
  - 后端需配合解密;若你的后端无此能力,把 `useEncryption: false` 即可走明文。

### 4.5 双内核 VPN 调度

入口 `UnifiedVpnService` 直接转发到 `V2rayService`,后者:

1. **桌面端**(Windows/macOS/Linux)`v2ray_service.dart` 第 78-119 行:
   - 先 `disconnect()`(防 file lock)→ `_findAvailablePort` 动态分配 SOCKS/HTTP 端口。
   - **TUN 模式**:走 `SingboxService.connectWithTun()`(sing-box 子进程)。
   - **普通模式**:走 `_startDesktopXray()`(xray 子进程 + 系统代理)。
2. **移动端** 第 122-167 行:
   - 把节点 Outbound 序列化为 Xray JSON,合并 `routingMode` + `routingRules`(注入 API 直连规则避免代理回环)。
   - 通过 MethodChannel `com.example.flux/v2ray` `connect` 把 JSON 字符串送到 Kotlin。
3. **状态广播**:桌面端用 `_desktopStatusController`,移动端订阅 `EventChannel('com.example.flux/v2ray_status')`。

#### Android 一侧 `MainActivity.kt`
- `connect`:
  - `getXrayExecutable()`(优先 `nativeLibraryDir/libxray.so`,fallback 拷 assets);
  - `buildV2rayConfig` 拼出完整 Xray JSON(注入双 inbound socks/http、域名 sniffing、CN 直连、自定义 `routingRules`);
  - 启动 Xray 进程 + 启日志线程 → `requestVpnPermission()` → `startService(FluxVpnService)`;
- `FluxVpnService.kt`:
  - 标准 `VpnService`,把 TUN fd 交给 **hev-socks5-tunnel** native 库(`libhev-socks5-tunnel.so`),库通过 `hev/htproxy/TProxyService.kt` 暴露 `TProxyStartService(configPath, fd)`、`TProxyStopService()`、`TProxyGetStats()`。
  - 监听 `defaultNetworkRequest` 设置 `setUnderlyingNetworks` 保证 IPv6/Wi-Fi 切换稳定。
- 三个 ABI 都打包在 `android/app/src/main/jniLibs/`:`libxray.so`、`libhev-socks5-tunnel.so`、`libhysteria2.so`(后者已在 Dart 层弃用,统一进 Xray);另有 `libv2ray.aar` 通过 `fileTree` 引入。

#### 桌面端
- 二进制位于 `assets/bin/{linux,macos,windows}`,首次启动时由 `_ensureDesktopAssets`(在 `v2ray_service.dart` 内,Win 平台 `bin` 子目录,Mac/Linux 直接同级)拷到 `getApplicationSupportDirectory()` 并 chmod。
- Windows 还带 `wintun.dll`(sing-box TUN 模式必需驱动)。
- macOS:`PROJECT_DIR/../assets/bin/xray-darwin-*` 由 `macos/copy_xray_bin.sh` 拷进 `.app/Contents/Resources/xray-bin/`(在 Xcode 的 Run Script Phase 中触发)。

#### iOS
- 仓库里只有**模板**:`ios/Runner/PacketTunnelProvider.swift` 顶部有六步 SETUP 注释,需要你在 Xcode 自己加 `Network Extension` Target 并把 V2Ray Core(LibXray.xcframework / Sing-box.xcframework)链接进去。Flux 仓库**没有提供 iOS 内核二进制**,这是商业版才补齐的部分。
- `VPNManager.swift` 提供 `NETunnelProviderManager` 包装,bundleId 取主 app + `.PacketTunnel`。

### 4.6 配置加密 `lib/utils/config_encryption.dart` ↔ `encrypt_config.py`

- 算法只是 **XOR + Base64**(不是 AES,虽然 README 标 AES-128;真正的 AES 只用于邮箱验证),目的是混淆而非密码学强度。
- 双端 KEY 必须**完全一致**:
  - Dart `_encryptionKey` 在 `config_encryption.dart` 第 9 行。
  - Python `KEY` 在 `encrypt_config.py` 第 10 行。
- 工作流:编辑 `release_config_plaintext.json` → `python encrypt_config.py` → 输出 `release_config.json`(密文)→ 上传到 OSS,App 自动按顺序拉取并 `smartDecrypt`。

---

## 五、二次开发实操

### 5.1 必改清单(白标 Day-1)

按需把 **6 处 `TODO`** 全部替换:

| 文件 | 行 | 替换为 |
|---|---|---|
| `lib/utils/config_encryption.dart` | 9 | 你的 24+ 字符密钥(注意与 Python 同步) |
| `encrypt_config.py` | 10 | 同上 |
| `lib/services/remote_config_service.dart` | 20 | 你的 OSS URL 列表(可放 2~3 个 CDN) |
| `lib/services/remote_config_service.dart` | 26 | 兜底 V2Board API 域名 |
| `lib/services/v2ray_service.dart` | 140 | 注入到分流规则的 API 域名(防代理回环) |
| `lib/services/v2board_api.dart` | 95 | 16 字符 AES key(或把所有 `useEncryption=true` 改 false) |
| `lib/services/crisp_service.dart` | 14 | 你的 Crisp Website ID(也可由远程配置覆盖) |

### 5.2 应用身份(包名/名称/图标)

- **Android applicationId**:`android/app/build.gradle.kts` `applicationId = "com.example.flux"`。改后 `com.example.flux` 这个包名空间可保留,但你**至少要同步**:
  - `AndroidManifest.xml` 中 `MainActivity`/`FluxVpnService` 的 `android:name` 全限定名;
  - Kotlin 文件 `package` 声明;
  - `MainActivity.kt` 第 22-23 行里的两条 channel 名 `com.example.flux/v2ray*` 必须和 Dart 端 `v2ray_service.dart` 第 19-22 行一致。直接全局改名前请在两端一起改。
- **iOS bundle id**:`ios/Runner.xcodeproj/project.pbxproj` 中 `PRODUCT_BUNDLE_IDENTIFIER`(Runner 与 PacketTunnel 都要改;后者必须是前者 `+ .PacketTunnel`,见 `VPNManager.swift` 第 12 行)。
- **Windows MSIX**:`pubspec.yaml` 第 116-122 行的 `msix_config`。
- **Linux**:`linux/CMakeLists.txt` 中 `APPLICATION_ID`,以及 `linux/flux.desktop.in`。
- **应用名 / 图标**:替换 `assets/images/app_icon.png` 与 `assets/icons/app_icon.{png,ico}`,`flutter pub run flutter_launcher_icons`。Windows 还要替 `windows/runner/resources/app_icon.ico`。

### 5.3 加新页面 / 新 API

新 API 例:
1. 在 `lib/services/v2board_api.dart` 添加方法:
   ```dart
   Future<Map<String, dynamic>> getMyCustom() => _get('/user/my/custom');
   ```
2. 如果有专属数据结构,在 `lib/models/` 新建 `my_custom.dart`,提供 `fromJson` 工厂。
3. 业务封装放 `lib/services/`(单例 + Stream/Future 暴露)。
4. 页面放 `lib/screens/`,在 `root_shell.dart` 的 Tab 列表加入(若是底部导航项)或从其它页面跳转。
5. 新文案:同步两份 ARB 后 `flutter gen-l10n`。

### 5.4 加新协议 / 修改 outbound

- 桌面端配置由 `v2ray_service.dart` 的 `_buildProxyOutbound()` (第 497-515 行) + 节点对象的 `node.toV2rayConfig()`(见 `lib/models/server_node.dart`)生成,这是改协议参数(如新增字段)的入口。
- 移动端 Xray 配置由 Kotlin 侧 `buildV2rayConfig()`(`MainActivity.kt` 第 346-496 行)。如果需要新协议(比如 `wireguard`),要同时改:
  1. `ServerNode` 解析(订阅解析在 `SubscriptionService`);
  2. Outbound 生成(模型上的 `toV2rayConfig` 或 `toSingboxOutbound`);
  3. 平台 native 层是否需要额外能力(WireGuard 需要 `kernel module` / `wg-go`)。
- TUN 模式相关 sing-box 配置生成在 `singbox_service.dart` 的 `_buildSingboxConfig`,这是另一份独立的 outbound 模板,**别忘了双份维护**。

### 5.5 改分流规则

- 开发期改 `routing_rules.json` → 上传 OSS → 在 `release_config` 的 `routing_rules.url` 字段指向它。规则**自上而下**匹配,`outboundTag` 取 `proxy / direct / block`。
- 真正的注入位置:
  - 移动端:`v2ray_service.dart` 第 144-158 行把规则插入 `routingRules` 数组并放在第 0 位强制 API 直连;
  - 桌面 Xray:`v2ray_service.dart` 第 344-372 行;
  - 桌面 sing-box:`singbox_service.dart` 的 `_loadCustomRules()`。

### 5.6 商业化模块定制

- **订阅**:`subscription_service.dart` 处理:V2Board 接口拿 `subscribe_url` → 兼容 `vmess://`/`vless://`/clash YAML 等多种格式 → 解析成 `List<ServerNode>`。要支持新订阅格式,扩展该文件即可。
- **支付**:页面在 `plans_screen.dart` / `orders_screen.dart` / `order_success_screen.dart`;后端走 V2Board 标准接口(下单/获取支付链接),App 通过 `url_launcher` 打开网页支付。
- **邀请**:`invite_screen.dart` + `invite_data.dart`。
- **客服**:`crisp_service.dart` 仅 Android/iOS 调用 `crisp_chat`;桌面端会落到 `url_launcher` 打开网页 Crisp。Crisp Website ID 优先取远程配置 `contact.crisp_website_id`,缺省回落 `crisp_service.dart` 第 14 行。
- **功能开关**:`release_config.features.{purchase_enabled, invite_enabled, ssr_enabled}` 控制 UI 显隐;在前端读 `RemoteConfigService` 的 `features` 字段即可。

### 5.7 多语言 / 主题

- 主题入口 `lib/theme/app_theme.dart`,颜色 `lib/theme/app_colors.dart`。当前只有 `AppTheme.dark()`;如要白模式,新增 `light()` 并在 `MaterialApp.theme/darkTheme/themeMode` 配合 `shared_preferences` 切换。
- Google Fonts 通过 `google_fonts` 直接拉,生产打包前最好预下载并改成离线字体以避开网络。

---

## 六、构建与发布

### 6.1 Android
1. 自己生成 keystore,放到 `android/app/key.jks`(注意仓库中的 `key.jks.bak` 仅备份,**不要提交真 key**)。
2. 创建 `android/key.properties`:
   ```
   storePassword=...
   keyPassword=...
   keyAlias=...
   storeFile=app/key.jks
   ```
3. `flutter build apk --release` 或 `flutter build appbundle`。
4. `proguard-rules.pro` 已存在但 `isMinifyEnabled = false`(见 `build.gradle.kts` 第 62-66 行);若要开启 minify,先在 proguard 里 keep 住 `hev.htproxy.TProxyService`、`com.example.flux.FluxVpnService`、libv2ray 的 JNI 入口,否则 native 调用会丢符号。

### 6.2 iOS
1. 在 Xcode 打开 `ios/Runner.xcworkspace`,**新建 Network Extension Target**(命名 `PacketTunnel`),把 `PacketTunnelProvider.swift` 拷进去。
2. 主 Target 与 Extension 都需要:
   - `Network Extensions` capability;
   - 同一个 App Group(可选,共享配置时用);
   - 团队签名匹配。
3. 你必须自备 V2Ray / Xray 内核 framework(LibXray.xcframework 等)并链接到 Extension Target。
4. `flutter build ipa`。

### 6.3 Windows
1. `flutter build windows --release`。
2. 用 Inno Setup 编译 `windows/flux_setup.iss`;脚本里 `[Files]` 段会把 `assets/bin/windows/sing-box.exe`、`xray.exe`、`wintun.dll` 一并打包(并不是放进 Flutter assets,见 `pubspec.yaml` 第 86-88 行的注释)。
3. TUN 模式需要管理员权限运行;sing-box 启动逻辑在 `singbox_service.dart` 已通过 PowerShell + UAC + `config.reload` 标志文件实现"守护重载"。

### 6.4 macOS
1. 在 Xcode 选 Runner Target 添加 Run Script Phase 调 `bash $PROJECT_DIR/copy_xray_bin.sh`。
2. 注意 `macos/Runner/Release.entitlements` / `DebugProfile.entitlements` 需要保留 `com.apple.security.network.client` / `network.server`,否则 Flutter HTTP 出不去。
3. 公证(notarize)时需把 xray、sing-box 二进制一并签名:`codesign --deep --force --sign "Developer ID..." Flux.app/Contents/Resources/xray-bin/*`。

### 6.5 Linux
- `flutter build linux --release`,二进制在 `build/linux/x64/release/bundle`。把 `assets/bin/linux/{sing-box,xray}` chmod +x 后随 bundle 分发。`AppImage`/`.deb` 可以自己再封一层。

---

## 七、调试要点 & 常见坑

1. **改 channel 名**:`com.example.flux/v2ray` 在 Dart 与 Kotlin 写死了两次,改包名时容易漏。建议保持原 channel 名只改包名前缀。
2. **Android `nativeLibraryDir` 找不到 xray**:`MainActivity.kt` 第 129-164 行已写了多级 fallback,但前提是 `jniLibs/` 三个 ABI 都要存在 `libxray.so`。否则 `flutter run` 时 `extractNativeLibs="true"` 也救不了。
3. **`extractNativeLibs="true"`** 是为了让 xray 二进制在文件系统上可执行(否则 Android 7+ 默认只能 mmap)。Manifest 里已显式打开,**别动它**。
4. **关闭窗口要清理 sing-box**:`main.dart` 第 121-144 行在 Windows 上额外 `taskkill /F /IM sing-box.exe`,因为 TUN 网卡进程死掉后 wintun 句柄不释放。**新增桌面平台的"右上角×"逻辑必经此处**。
5. **API 直连规则**:`v2ray_service.dart` 第 140 行写死了 `domain:your-api-domain.com`,你换品牌时一定要替换为真实 API 域名,否则规则模式下 API 走代理 → 代理失败 → 节点拉不下来 → 死循环。
6. **OSS 配置版本号**:`release_config.config_version` 必须每次 +1,客户端否则缓存命中不会刷。
7. **DNS 回环**:Android Xray 会注入 `direct: 8.8.8.8 / 1.1.1.1`(`MainActivity.kt` 第 447 行)。如果你换 DNS,一并修改这个 break-loop 规则。
8. **TUN 模式与系统代理**:桌面 Xray 模式会 `_setDesktopProxy(true)` 启用系统代理,TUN 模式会跳过(让 sing-box 接管)`v2ray_service.dart` 第 443-447 行。两种模式切换时务必先 `disconnect`,否则系统代理设置和 TUN 网卡会冲突。
9. **节点延迟探测**:`latency_test_service.dart` 默认通过本地 TCP connect 探测;商业站点常见的 CDN 节点延迟会被 anycast 干扰,需要时可改为通过当前已连接代理探测目标 URL 时延。
10. **`crisp_chat` 不支持桌面**:`pubspec.yaml` 第 52 行注释已说明。桌面端要回退到 `url_launcher` 打开 Crisp web,否则 release 编译会报 missing plugin。
11. **测试覆盖近乎为零**:`test/` 下只有占位的 `widget_test.dart`,二次开发时建议补 `unit test`(尤其 `RemoteConfigService` 的解密分支与 `SubscriptionService` 的多格式解析)。

---

## 八、最小可跑流程(Quick Start Checklist)

```
[ ] 替换 6 处 TODO(密钥、OSS、域名、API key)
[ ] 编辑 release_config_plaintext.json → python encrypt_config.py → 上传 OSS
[ ] 编辑 routing_rules.json → 上传 OSS
[ ] 修改包名/应用名/图标
[ ] flutter pub get && flutter gen-l10n
[ ] Debug:flutter run -d <device>
[ ] Release:
    - Android  flutter build apk --release   (先准备 key.properties)
    - Windows  flutter build windows --release && Inno Setup 编 setup
    - macOS    Xcode + copy_xray_bin.sh
    - iOS      Xcode 加 PacketTunnel Target、链接 LibXray.xcframework、archive
    - Linux    flutter build linux --release
[ ] 验证:登录 → 拉订阅 → 切节点 → TUN 模式 / 普通模式分别连一遍
```

---

## 九、推荐的扩展方向

- **state management 升级**:目前是 setState + 单例 service,业务复杂后建议引入 `riverpod` / `bloc`。
- **多端深链 / Universal Link**:`url_launcher` 已用,可在 V2Board 支付返回页加自定义 scheme(如 `flux://order-success?id=xxx`),通过 `app_links` 接收并跳到 `order_success_screen.dart`。
- **CI**:仓库无 `.github/workflows/`,建议补一个 `flutter analyze + test + build apk(matrix 三 ABI)`,以及 Windows / macOS 的 GitHub Actions runner。
- **真正的 AES-128 替换 XOR**:把 `config_encryption.dart` 换成 `package:encrypt` 的 AES-CBC + IV(Python 端用 `pycryptodome` 同步实现)以提高门槛。
